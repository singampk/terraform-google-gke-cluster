# This build file is to build a Docker machine image to run the terraform code

steps:
  - id: "Docker builder"
    name: "gcr.io/cloud-builders/docker"  
    args: ['build', '-t', 'gcr.io/fabled-autonomy-268009/codebuild:latest', '.']